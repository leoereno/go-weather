import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    clearBtn:{
        backgroundColor: 'grey',
        alignItems: 'center',
        margin: 10,
        padding: 10,
    },
    clearBtnTxt: {
        fontSize: 18
    }
})